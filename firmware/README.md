# Firmware

Firmware for the PIC32MX534F064H programmer and validation board.

## Directories

- `bootloader/` — MPLAB X / XC32 USB bootloader project
- `examples/` — Hardware-validation and peripheral test applications

MPLAB build outputs, private IDE settings, object files, and generated binaries are excluded through the repository `.gitignore`.
